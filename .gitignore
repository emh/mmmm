# secrets — never commit
.env
.dev.vars
*.key
openai-key

# generated art
# Raw model sheets are large (~2MB each) and regenerable from art/*/art.json.
# The cut sprites in assets/ ARE committed -- they're the deliverable, and the
# bake-off page is useless without them.
art/sheets/

# os / editor
.DS_Store
*.swp
.vscode/
.idea/

# python
__pycache__/
*.pyc
